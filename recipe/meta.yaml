{% set name = "dioptas" %}
{% set version = "0.8.3" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/dioptas-{{ version }}.tar.gz
  sha256: 6e57ee0db02cdeeb2d6a821ca0fe92361b4294df9ec52c15980599d7d516eb36

build:
  noarch: python
  entry_points:
    - dioptas = dioptas:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - poetry-core
    - poetry-dynamic-versioning
  run:
    - python >={{ python_min }}
    - h5py
    - hdf5plugin
    - lmfit
    - pandas
    - psutil
    - pycifrw
    - pyqt  # Use pyqt6 when available in conda-forge
    - pyfai >=2025.12.1
    - pyqtgraph
    - pyshortcuts
    - qtpy
    - scikit-image
    - watchdog
    - pyopengl
    - xypattern
    - numpy
    - scipy
    - numexpr
    - qt-material
    - bitshuffle >=0.5.1
    # Optional dependency missing:
    # - extra-data
    # - pyopengl-accelerate
    # dioptas_batch dependency missing:
    # - sharedmem

test:
  requires:
    - python {{ python_min }}
  imports:
    - dioptas

app:
  entry: dioptas
  summary: "Dioptas"
  icon: icon.png

about:
  home: https://github.com/Dioptas/Dioptas
  license: MIT
  license_file: LICENSE.txt
  summary: GUI program for reduction and exploration of 2D X-ray diffraction data
  doc_url: https://dioptas.readthedocs.io/en/stable/
  dev_url: https://github.com/dioptas/dioptas

extra:
  recipe-maintainers:
    - CPrescher
    - t20100
